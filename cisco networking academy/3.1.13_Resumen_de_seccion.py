# Resumen de Sección
"""

Los operadores de comparación (o también denominados operadores relacionales) se utilizan para comparar valores. La siguiente tabla ilustra cómo funcionan los operadores de comparación, asumiendo que.

x = 0, y = 1, Y z = 0:

Operadores:

==  Devuelte True si los valores de los operadores son iguales, y False de lo contrario.
Ejemplo: x == y # False, x == z # True

!=  Devuelve True si los valores de los operandos NO son iguales, y False de lo contrario.
Ejemplo: x != y # True, x != z # False

>   Devuelve True si el valor del operando izquierdo es mayor que el valor del operando derecho, y False de locontrario.
Ejemplo: x > y # False, y > z # True

<   Devuelve true si el valor del operando izquierdo es menor que el valor del operando derecho, False de lo contrario.
Ejemplo: x == y # True, y < z # False

>=  Devuelve true si el valor del operando izquierdo es mayor o igual al valor del operando derecho, y False de lo contrario.
Ejemplo: x >= y # False, x >= z # True, y >= z # True

<=  Devuelve True si el valor del operando izquierdo es menor o igual al valor del operando derecho, y False de lo contrario.
Ejemplo: x <= y # True, x <= z # True, y <= z # False



"""